---
page_title: "Nscale: nscale_kubernetes_cluster"
subcategory: ""
description: |-
  Nscale Kubernetes Cluster
---

# Data Source: nscale_kubernetes_cluster

Reads a Nscale Kubernetes Service (NKS) cluster by ID.

Use this to reference a cluster that Terraform does not manage — for example, to point the `kubernetes` provider at a
cluster created by the `nscale` CLI or another workspace.

-> **The NKS endpoint must be configured.** NKS has no default endpoint baked into the provider yet. Set
`nks_service_api_endpoint` on the provider, or the `NSCALE_NKS_SERVICE_API_ENDPOINT` environment variable, before
using this data source.

## Example Usage

```hcl
data "nscale_kubernetes_cluster" "existing" {
  id = "b2c8d4e6-1a3f-4c7b-9e2d-5f8a0c1b3d7e"
}

output "api_endpoint" {
  value = format(
    "https://%s:%s",
    data.nscale_kubernetes_cluster.existing.api_server_endpoint.private_host,
    data.nscale_kubernetes_cluster.existing.api_server_endpoint.private_port,
  )
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) The unique identifier of the cluster to look up.

### Read-Only

- `addons` (Attributes) Addon profiles enabled on the cluster. (see [below for nested schema](#nestedatt--addons))
- `api_server` (Attributes) Network exposure configured for the cluster's Kubernetes API server. (see [below for nested schema](#nestedatt--api_server))
- `api_server_endpoint` (Attributes) Credential-free connection data for the cluster's Kubernetes API server. Null until the control plane is reachable. (see [below for nested schema](#nestedatt--api_server_endpoint))
- `applied_platform_release_id` (String) The platform release last observed as applied to the cluster.
- `cluster_network` (Attributes) Pod and service network CIDRs for the cluster. (see [below for nested schema](#nestedatt--cluster_network))
- `creation_time` (String) The timestamp when the cluster was created.
- `description` (String) The description of the cluster.
- `eligible_upgrade_target_ids` (List of String) Eligible platform release IDs, in upgrade order.
- `health_status` (String) The health state of the cluster.
- `kubernetes_version_observed` (String) The Kubernetes version reported by the managed control plane.
- `kubernetes_version_target` (String) The Kubernetes version applied to the cluster's control plane.
- `name` (String) The name of the cluster.
- `network_id` (String) The identifier of the region network the cluster attaches to.
- `organization_id` (String) The identifier of the organization the cluster belongs to.
- `platform_release_deprecated` (Boolean) Whether the applied platform release is currently deprecated.
- `platform_release_id` (String) The identifier of the NKS platform release the cluster runs.
- `platform_release_withdrawn` (Boolean) Whether operators have withdrawn the applied platform release.
- `project_id` (String) The identifier of the project the cluster belongs to.
- `provisioning_status` (String) The provisioning state of the cluster.
- `region_id` (String) The identifier of the region the cluster is provisioned in.
- `tags` (Map of String) A map of tags assigned to the cluster.
- `upgrade_available` (Boolean) Whether at least one eligible platform release upgrade target was observed.

<a id="nestedatt--addons"></a>
### Nested Schema for `addons`

Read-Only:

- `hardware` (Boolean) Whether the optional hardware addon profile is enabled.


<a id="nestedatt--api_server"></a>
### Nested Schema for `api_server`

Read-Only:

- `allowed_cidrs` (Set of String) Source IPv4 CIDR allowlist for the cluster API endpoint.
- `public_ip` (Boolean) Whether the API server is exposed through a public endpoint.


<a id="nestedatt--api_server_endpoint"></a>
### Nested Schema for `api_server_endpoint`

Read-Only:

- `certificate_authority_data` (String) The complete Kubernetes API server CA bundle, base64-encoded.
- `private_host` (String) The address of the private API server endpoint.
- `private_port` (Number) The TCP port of the private API server endpoint.
- `public_host` (String) The address of the public API server endpoint.
- `public_port` (Number) The TCP port of the public API server endpoint.


<a id="nestedatt--cluster_network"></a>
### Nested Schema for `cluster_network`

Read-Only:

- `pod_cidr` (String) IPv4 CIDR used for Kubernetes pod addresses.
- `service_cidr` (String) IPv4 CIDR used for Kubernetes service addresses.

## Notes

* Lookup is by `id` only. The NKS list API filters names exactly and case-sensitively, and display names are not
  unique, so name-based lookup would be ambiguous.
* `api_server_endpoint` is null until the cluster's control plane is reachable. Reading a cluster that is still
  provisioning returns null for it, and for `kubernetes_version_*` and the platform-release status attributes.
* `certificate_authority_data` is the cluster's public CA bundle, not a secret.
* See the [`nscale_kubernetes_cluster` resource](../r/kubernetes_cluster.html) for how to hand these values to the
  `kubernetes` and `helm` providers.
